# Production value is managed through Sites, never bundled into client JavaScript.
ADMIN_EMAIL=editor@example.invalid
# Public URL, bundled at build time. Local Cloud can use http://localhost:3000.
VITE_VIBE_ORIGIN=https://console.vibecloud.se
